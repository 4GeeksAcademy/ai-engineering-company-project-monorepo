# TrackFlow Backoffice UI

Minimal frontend for incident analysis.

## Run (static)

From repository root:

```bash
python -m http.server 5500 --directory uis/backoffice
```

Open `http://127.0.0.1:5500` and set API URL to your backend (default `http://127.0.0.1:8000`).
